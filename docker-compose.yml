services:
  # ----------------- Spark / JupyterLab -----------------
  spark:
    build: .
    container_name: tradecorp_training_spark
    env_file:
      - .env
    ports:
      - "8888:8888"
      - "4040:4040"
    volumes:
      - .:/home/jovyan/work
  

